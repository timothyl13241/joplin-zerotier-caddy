# Cloudflare API token scoped to Zone.DNS:Edit for your domain
CF_API_TOKEN=YOUR_CLOUDFLARE_ZONE_DNS_EDIT_TOKEN

# The server's ZeroTier IP (after the zerotier service is joined and authorized)
ZT_IP=10.147.17.10
